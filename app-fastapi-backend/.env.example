# URL del frontend React
# En desarrollo local usa: http://localhost:5173 (o el puerto que uses con Vite)
# En producción usa la URL donde esté desplegado tu frontend
FRONTEND_URL=http://localhost:5173

# Clave secreta para sesiones (CAMBIA ESTO EN PRODUCCIÓN)
SECRET_KEY=tu-super-secreto-key-debe-cambiarse
